# Healthcare Privacy Firewall — Local Development Requirements
# Lightweight subset for running the gateway without Docker

# FastAPI Gateway
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
httpx>=0.25.0

# Detection (core)
presidio-analyzer>=2.2.350
presidio-anonymizer>=2.2.350
spacy>=3.7.0

# Redis Queue (optional — gateway degrades gracefully)
redis>=5.0.0

# Utilities
pyyaml>=6.0.1
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Image (optional — Pillow is lightweight)
Pillow>=10.0.0

# Alerts
slack-sdk>=3.26.0

# Analytics
prometheus-client>=0.19.0
